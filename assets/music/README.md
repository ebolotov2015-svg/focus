# Music assets

This directory is intentionally empty.

Do not commit copyrighted music. Users can add their own audio files with:

```bash
focus music add ~/Music/rain.ogg
```

Supported formats are `.mp3`, `.wav`, and `.ogg`, depending on local audio support from `pygame`.
